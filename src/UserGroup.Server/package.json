{
  "scripts": {
    "tailwind": "cross-env NODE_ENV=development ./node_modules/tailwindcss/lib/cli.js -i ./styles/site.css -o ./wwwroot/css/site.css --watch",
    "copy-img": "copyfiles -u 1 images/*.* images/**/*.* wwwroot/img",
    "clean-img": "rimraf wwwroot/img"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "rimraf": "^5.0.7",
    "tailwindcss": "^3.4.3"
  }
}